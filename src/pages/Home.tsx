import { TaskForm } from "@/components/form/TaskForm";
import { TodoWrapper } from "@/components/TodoWrapper";

export const Home = () => {
    return (
        <>
            <TaskForm />
            <TodoWrapper />
        </>
    );
};
